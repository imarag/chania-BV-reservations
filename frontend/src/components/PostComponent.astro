<!-- ---
import { getUserById } from "@utils/db-utils";

const { post } = Astro.props;
const authorId = post.userId;
const author = await getUserById(authorId);
const authorEmail = author.email;
---

<div class="post">
  <h3>{post.title}</h3>
  <p>{post.body}</p>
  <p class="text-sm">by {authorEmail}</p>
</div>

<style>
  .post {
    max-width: 800px;
    margin-inline: auto;
    margin-block: 20px;
    border: 1px solid black;
    background-color: aqua;
    border-radius: 10px;
    padding: 20px;
  }

  .post h3 {
    text-align: center;
  }

  .post p {
    font-weight: 100;
  }
</style> -->
